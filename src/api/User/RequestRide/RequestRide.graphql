type RequestRideResponse {
  ok: Boolean!
  error: String
  ride: Ride
  chat: Chat
}


type Mutation {
  RequestRide(
    fromId: Int!
    toId: Int!
    expectingFee: Float!
    distance: String
    duration: String
    creditId: Int!
  ): RequestRideResponse!
}
