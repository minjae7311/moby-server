type RequestRideResponse {
  ok: Boolean!
  error: String
  ride: Ride
}

input PlaceInput {
  id: Int!
}

type Mutation {
  RequestRide(
    from: PlaceInput!
    to: PlaceInput!
    finalFee: Float
    distance: String
    duration: String
  ): RequestRideResponse!
}
