import Ride from "../entities/Ride";

export const handleFindingDistance = (ride: Ride) => {
    /**
     * @todo sleep, and reload ride 
     *       if ride is still requesting,
     *       then findDistance += 10 and resend to pubSub channel..
     */
};
